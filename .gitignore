# Environment variables
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*

# Config local do Claude Code -- settings.local.json guarda permissoes com
# API keys/tokens em texto puro, nunca deve ir pro historico do git.
.claude/settings.local.json

# Scratch da ferramenta de brainstorm visual (superpowers)
.superpowers/

# Cache local do Supabase CLI
supabase/.temp/
.tmp-migrations-sync/

# Saída gerada da carga única de mensagens do funil da Turma #38 (286 arquivos).
# O gerador é scripts/gen-sql-38.mjs e o resultado já está em funnel_messages no banco
# (67 mensagens, 65 enviadas). Ficam no disco, fora do repositório.
scripts/batch*.sql
scripts/all_batches.txt
scripts/b[0-9].json
scripts/funil38.sql

# Exportações de campanha: contêm nome e telefone de lead. Nunca vão para o repositório.
.campanha-*.csv

# Estado local do CLI do Supabase.
supabase/.branches/
